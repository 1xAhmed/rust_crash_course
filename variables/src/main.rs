fn main() {
    // without type annotation
    let bunnies = 2;

    // with type annotation
    let sunny: i32 = 4;

    // initalize multiple variables at once
    let (apple, banana) = (8, 5);

    
}
