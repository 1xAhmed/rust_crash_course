// You use cargo to search for install and manage packages that you want to use cargo is also the build system no more. Cargo is also the test Runner and the documentation generator Etc. It's like all of the good parts of npm and pip and bundler and make


// Create a new rust project with 
// `cargo new hello`

// Run it via
// `cargo run` after getting into the hello dir in cli.

fn main() {
    println!("Hello, world!");
}
